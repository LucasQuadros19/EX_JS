let base;
let altura;
let resultado;
base=prompt("qual o tamanho da base em cm:");
altura=prompt("qual a altura em  em cm:");
base=parseInt(base);
altura=parseInt(altura);
resultado = base*altura;
alert("area do retangulo:"+resultado+"cm");
